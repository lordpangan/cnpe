apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: java-ci
spec:
  params:
    - name: repo_url
    - name: revision
    - name: image-reference
  workspaces:
    - name: source
    - name: basic-auth
    - name: maven-settings
  tasks:
    - name: fetch-repository
      taskRef:
        name: git-clone
      workspaces:
        - name: output
          workspace: source
        - name: basic-auth
          workspace: basic-auth
      params:
        - name: url
          value: $(params.repo_url)
        - name: revision
          value: $(params.revision)

    # Linting — Spotless / Google Java Format. Reuses the maven Task
    # with a different GOALS value. Runs first because format drift is the
    # cheapest failure to produce and the cheapest to fix locally
    # (`mvn spotless:apply`).
    - name: lint
      runAfter: [fetch-repository]
      taskRef:
        name: maven
      workspaces:
        - name: source
          workspace: source
        - name: maven-settings
          workspace: maven-settings
      params:
        - name: GOALS
          value: ["spotless:check"]
        - name: MAVEN_IMAGE
          value: docker.io/library/maven:3.9.6-eclipse-temurin-21@sha256:8d63d4c1902cb12d9e79a70671b18ebe26358cb592561af33ca1808f00d935cb

    # Domain 1 (GitOps & CD) — run unit tests before any image work so
    # test failures fail the pipeline cheaply, independent of the build.
    - name: unit-test
      runAfter: [lint]
      taskRef:
        name: maven
      workspaces:
        - name: source
          workspace: source
        - name: maven-settings
          workspace: maven-settings
      params:
        - name: MAVEN_IMAGE
          value: docker.io/library/maven:3.9.6-eclipse-temurin-21@sha256:8d63d4c1902cb12d9e79a70671b18ebe26358cb592561af33ca1808f00d935cb

    # Domain 5 (Security & Policy) — static analysis on the source tree.
    # Runs after tests so an obviously broken build fails first.
    - name: sast
      runAfter: [unit-test]
      taskRef:
        name: semgrep
      workspaces:
        - name: source
          workspace: source

    # Domain 1 — build multi-stage image via Kaniko and push to the
    # in-cluster registry. --insecure because kind-registry:5000 is HTTP.
    # IMAGE_URL + IMAGE_DIGEST results are picked up by Tekton Chains
    # cluster-wide config for automatic SLSA provenance + signing.
    - name: image-build
      runAfter: [sast]
      taskRef:
        name: kaniko
      workspaces:
        - name: source
          workspace: source
      params:
        - name: IMAGE
          value: $(params.image-reference)
        - name: EXTRA_ARGS
          value: ["--insecure", "--ignore-path=/product_uuid"]
        - name: BUILDER_IMAGE
          value: gcr.io/kaniko-project/executor:v1.24.0@sha256:4e7a52dd1f14872430652bb3b027405b8dfd17c4538751c620ac005741ef9698
